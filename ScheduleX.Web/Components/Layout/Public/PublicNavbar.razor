@* <h3>PublicNavbar</h3> *@

@* @code { *@

@* } *@


<nav class="nav">
    <div class="nav-inner">

        <a class="brand" href="/">
            <img class="brand-logo" src="images/logo.png" alt="ScheduleX" onerror="this.style.display='none'" />
            <span class="brand-text">ScheduleX</span>
        </a>

        <div class="nav-links">
            <a href="#features">Features</a>
            <a href="#how">How it works</a>
            <a href="#faq">FAQ</a>
        </div>

        <div class="nav-actions">
            @* <a class="btn ghost" href="/admin">Admin</a> *@
            <NavLink class="btn secondary" href="/login">Admin</NavLink>
            <a class="btn primary" href="#demo">Try demo</a>
        </div>

        <button class="nav-burger" type="button" @onclick="Toggle">
            ☰
        </button>
    </div>

    @if (isOpen)
    {
        <div class="nav-mobile">
            <a href="#features" @onclick="Close">Features</a>
            <a href="#how" @onclick="Close">How it works</a>
            <a href="#faq" @onclick="Close">FAQ</a>
            @* <a class="btn ghost" href="/admin" @onclick="Close">Admin</a> *@
            <NavLink class="btn ghost" href="/login">Login</NavLink>
            <a class="btn primary" href="#demo" @onclick="Close">Try demo</a>
        </div>
    }
</nav>

@code {
    private bool isOpen;

    private void Toggle() => isOpen = !isOpen;
    private void Close() => isOpen = false;
}